apiVersion: v1
kind: Service
metadata:
  name: storevault-internal-service
  namespace: default
spec:
  selector:
    app: storevault-app 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000 
  type: ClusterIP