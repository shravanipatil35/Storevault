apiVersion: v1
kind: Service
metadata:
  name: storevault-internal-service
  namespace: default
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: storevault-web